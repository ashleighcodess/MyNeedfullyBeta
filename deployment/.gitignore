# Only ignore actual sensitive files that will be created during deployment
# Allow all deployment scripts and templates to be committed

# Actual environment files with real secrets (not templates)
.env
.env.local

# Runtime files created during deployment
*.log
pids/
*.pid

# SSL certificates created during deployment
*.pem
*.key
*.crt

# Database backups created during deployment
backup_*.sql

# OS files
.DS_Store
Thumbs.db